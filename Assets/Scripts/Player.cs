using Unity.Entities;

// As a matter of convention, entity component types are often defined in the same file
// as a related authoring component, but in this case the Player component doesn't need
// an authoring component.
public struct Player : IComponentData
{
        
}